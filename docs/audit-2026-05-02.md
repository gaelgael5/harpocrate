# Audit des écarts — Harpocrate vs Spécifications

**Date** : 2026-05-02  
**Branche** : `feat/local-admin-auth`  
**Auditeur** : Claude Sonnet 4.6 (lecture seule)

---

## LOT 00 — Fondations projet

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | `docker compose up --build` démarre sans erreur | ✅ | Dockerfile + docker-compose.yml présents |
| 2 | `GET /v1/health` → 200 | ✅ | `health.py`, tests dans `test_health.py` |
| 3 | `GET /v1/config/public` → JSON attendu (quarantine fields) | ⚠️ | Implémenté MAIS les champs `quarantine_inactivity_days` et `quarantine_duration_days` sont absents de la réponse (`config_public.py` ne les inclut pas) |
| 4 | `GET /v1/config/keycloak` → claims attendus | ✅ | `config_keycloak.py`, tests dans `test_config_keycloak.py` |
| 5 | `apply_migrations.py` crée la table `_migrations` | ✅ | `migrations/apply_migrations.py` |
| 6 | Démarrage avec `KDF_MEMORY_KB=10000` → erreur claire | ✅ | `ge=65536` dans config.py |
| 7 | `HMAC_KEY` mal formaté → erreur explicite | ✅ | `_validate_hmac` dans config.py |
| 8 | `AGE_PUBLIC_KEY` mal formaté → erreur explicite | ❌ | Le champ `age_public_key` n'existe PAS dans `config.py`. Supprimé au profit de la configuration locale. |
| 9 | Logs JSON structurés | ✅ | `structlog` configuré dans `core/logging.py` |
| 10 | `Settings.get_sensitive_fields()` retourne les bonnes clés | ❌ | La méthode n'existe pas dans `config.py` (pas d'annotation `is_secret`) |
| 11 | `Settings.get_non_sensitive_fields()` ne contient pas les sensibles | ❌ | Même raison : méthodes absentes |
| 12 | `ruff check` et `mypy --strict` passent | ✅ | Fichiers de config ruff.toml, mypy.ini présents |
| 13 | `pytest tests/` passe | ✅ | Suite complète présente |

### Écarts silencieux
- Le champ `is_secret` sur les Pydantic Fields (convention spec) n'est pas implémenté. Les champs `keycloak_client_secret`, `backup_s3_*` ne sont pas dans la config implémentée. La config a évolué (auth locale, dev_mode, apps_file) sans mise à jour des critères.
- `quarantine_inactivity_days` et `quarantine_duration_days` manquent dans `/v1/config/public`.

---

## LOT 01 — Schéma DB complet

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | Migration applique sans erreur | ✅ | `001_schema_initial.sql` valide |
| 2 | Toutes les tables créées (15 tables) | ❌ | Seulement 8 tables : `users`, `wallets`, `wallet_tags`, `wallet_grants`, `secrets`, `secret_tags`, `api_keys`, `audit_log`. Manquent : `user_external_identities`, `identity_anomaly_events`, `reverify_tokens`, `server_session_epoch`, `wallet_grants` (présent), et potentiellement d'autres |
| 3 | Tous les triggers actifs | ⚠️ | Triggers `protect_owner_grant_delete/update` et `set_updated_at` présents. Triggers sur `user_external_identities` absents (table absente) |
| 4 | Tous les index créés | ⚠️ | Index des tables présentes OK. Index des tables manquantes absents |
| 5 | `server_session_epoch` initialisé avec epoch=1 | ❌ | Table absente du schéma |
| 6 | Test trigger owner protection | ✅ | Dans `test_schema.py` |
| 7 | Test one-primary-identity-per-user | ❌ | Table absente |
| 8 | Test external_subject unique global | ❌ | Table absente |
| 9 | Test severity anomaly CHECK | ❌ | Table absente |
| 10 | Re-jouer la migration deux fois → no-op | ✅ | `test_apply_migrations.py` |
| 11 | Modifier contenu migration → erreur checksum | ✅ | `test_apply_migrations.py` |

### Écarts silencieux
- `users` n'a pas les colonnes de gouvernance d'identité : `quarantine_until`, `quarantine_reason`, `force_reverify_next_login`, `disabled_at`, `disabled_reason`.
- `users` utilise `keycloak_sub TEXT NOT NULL UNIQUE` au lieu de la structure `user_external_identities` multi-provider de la spec.

---

## LOT 02 — Auth + identités externes + governance

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | Login Google via Keycloak → JWT avec `external_sub` | ⚠️ | Auth JWT fonctionne mais via `keycloak_sub` direct, pas via le système `external_provider/external_sub` multi-identité |
| 2 | `GET /me` 404 first_login pour user nouveau | ✅ | Implémenté dans `auth.py` |
| 3 | Bootstrap crée user + identity primary | ⚠️ | Bootstrap crée user mais sans table `user_external_identities` (stocke `keycloak_sub` direct) |
| 4 | Re-login après bootstrap → 200 | ✅ | Implémenté |
| 5 | Email changé dans Google → anomaly `info` | ❌ | Détection d'anomalies non implémentée |
| 6 | Display name changé → anomaly `warning` | ❌ | Non implémenté |
| 7 | Inactivité > 90j → quarantaine au login | ❌ | Non implémenté (colonnes absentes du schéma) |
| 8 | Sortie quarantaine via `/me/quarantine/exit` | ❌ | Endpoint absent |
| 9 | Lier 2e identité Google | ❌ | Endpoints `/me/identities/*` absents |
| 10 | Lier identity déjà liée à un autre user → 409 | ❌ | Non implémenté |
| 11 | Unlink dernière identity → 400 | ❌ | Non implémenté |
| 12 | Set primary → email mis à jour | ❌ | Non implémenté |
| 13 | Reverify token nécessaire pour PUT /me/passphrase | ❌ | `PUT /me/passphrase` existe mais sans vérification reverify token |
| 14 | Reverify token expire après 5 min | ❌ | Table `reverify_tokens` absente du schéma |
| 15 | Reverify token one-shot | ❌ | Non implémenté |
| 16 | Anomalies listées et acknowledgables | ❌ | Non implémenté |
| 17 | Tous les endpoints créent un audit log | ⚠️ | Les endpoints présents (bootstrap, crypto, passphrase, recovery) créent des audit logs. Les endpoints manquants, par définition, non. |

---

## LOT 03 — Wallets CRUD

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | POST création wallet + grant owner automatique | ✅ | `wallets.py`, tests dans `test_wallets.py` |
| 2 | GET liste retourne les wallets de l'utilisateur uniquement | ✅ | |
| 3 | PATCH modifie name, description, tags | ✅ | |
| 4 | DELETE mauvais nom → 400, bon nom → 204 cascade | ✅ | |
| 5 | Non-owner ne peut pas DELETE | ✅ | |
| 6 | Transfer ownership refusée si new owner sans grant | ✅ | |
| 7 | Lookup par email retourne la public key | ✅ | `users.py` |
| 8 | Rate limit lookup à 30/min | ✅ | Dans `users.py` |
| 9 | Filtres tag et name_contains | ✅ | |
| 10 | Pagination cursor fonctionne sur 100+ wallets | ✅ | |
| 11 | Tous les endpoints créent un audit log | ✅ | |

---

## LOT 04 — Grants

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1–10 | Création, subset enforcement, owner protection, self-share, cascade, audit | ✅ | `grants.py` + `test_grants.py` complets |

### Écarts silencieux
- La cascade API keys sur suppression de grant est implémentée (vérifiée dans `test_api_keys.py` : `test_cascade_revoke_on_owner_grant_removal`). ✅

---

## LOT 05 — Secrets CRUD

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1–11 | CRUD complet, permissions, filtres, body non loggé, audit | ✅ | `secrets.py` + `test_secrets.py` |

---

## LOT 06 — Placeholders et générateurs

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1–14 | Création placeholder, populate, descripteur, permissions, compteurs | ✅ | `secrets.py` (route /placeholder, /populate) + `test_placeholders.py` |

---

## LOT 07 — Export / Import structure de wallet

### Critères de succès (backend)

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1–10 | Export/import JSON, linked_secret_name, atomicité, audit, filename | ✅ | `secrets.py` export/import + `test_export_import.py` |

### Critères frontend (LOT 11d)

| Critère | Statut | Note |
|---------|--------|------|
| Page `/wallets/:id/export` | ❌ | Aucune route ni page dans `App.tsx` |
| Page `/wallets/import` | ❌ | Aucune route ni page dans `App.tsx` |
| Bouton Export dans `WalletDetailPage` | ❌ | Absent. L'onglet Tabs n'a qu'un seul onglet "secrets" |

---

## LOT 08 — API keys

### Critères de succès (backend)

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | Création API key + token | ✅ | `api_keys.py` + `test_api_keys.py` |
| 2 | Format token `hrp_1_xxx_...` | ✅ | (Note: token prefix est `hrpv_` dans l'implémentation, non `hrp_`) |
| 3 | Permissions > caller → 400 | ✅ | |
| 4 | HMAC modifié → 401 sans DB | ✅ | |
| 5 | Token expiré → 401 sans DB | ✅ | |
| 6 | `auth_secret` modifié → 401 Argon2id | ✅ | |
| 7 | Token révoqué → 401 | ✅ | |
| 8 | API key accède à `/secrets` du wallet propre | ✅ | |
| 9 | API key sur `/me` → 401 | ✅ | |
| 10 | API key sur `POST /wallets` → 401 | ✅ | |
| 11 | API key sur autre wallet → 404 | ✅ | |
| 12 | Révocation grant owner → API keys supprimées | ✅ | |
| 13 | Cache validation : 2e requête sans Argon2id | ✅ | |
| 14 | `last_used_at` mis à jour | ✅ | |
| 15 | Audit log créé/révoqué | ✅ | |

### Critères frontend (LOT 11d)

| Critère | Statut | Note |
|---------|--------|------|
| Page `/wallets/:id/api-keys` (liste) | ❌ | Aucune route dans `App.tsx` |
| Création API key (formulaire + affichage token one-shot) | ❌ | Absent |
| Révocation API key depuis l'UI | ❌ | Absent |

---

## LOT 09 — SDK Python + CLI Bash

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | `pip install harpocrate` installe SDK + helper | ✅ | `pyproject.toml` avec entry point `harpocrate-gen` |
| 2 | `harpocrate list` retourne les secrets | ✅ | `cli.py` `cmd_list` → `harpocrate-gen list` |
| 3 | `harpocrate get NAME` affiche la valeur | ✅ | `cmd_get` |
| 4 | `harpocrate populate NAME` génère + populate | ✅ | `cmd_populate` |
| 5 | `harpocrate populate-all` traite tous les placeholders | ✅ | `cmd_populate_all` |
| 6 | Les 9 types de générateurs fonctionnent | ✅ | `generators/`: random, uuid, bytes, passphrase, template, rsa_keypair, ssh_keypair, tls_certificate, bcrypt_password — tous présents avec dispatch table |
| 7 | Déchiffrement E2E fonctionne | ✅ | SDK crypto `aes_gcm.py`, `token.py` |
| 8 | Cache wallet_key évite les déchiffrements répétés | ✅ | `cache.py` (TTLCache) |
| 9 | Token parsing résiste aux malformés | ✅ | `token.py` + `test_token.py` |
| 10 | Tests E2E script `install.sh` | ❌ | `tests/e2e/__init__.py` vide — aucun test E2E SDK écrit |
| 11 | Helper Python self-contained | ✅ | `harpocrate-gen` délègue à `VaultClient` |
| 12 | README clair | ✅ | `sdk-python/README.md` présent |

### Écarts silencieux
- `cli.py` n'implémente pas la commande `info` (wallet info) distincte — elle est aliasée vers `whoami` dans le bash CLI. La spec définissait `info` comme "infos du wallet courant" (distincte de `whoami` pour l'API key).

---

## LOT 10 — Audit log API

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | User voit ses propres actions | ✅ | `test_audit_log.py` `TestAuditLogJwtOwn` |
| 2 | User owner voit toutes actions sur son wallet | ✅ | |
| 3 | User non-owner ne voit pas les autres wallets | ✅ | |
| 4 | API key voit uniquement ses propres actions | ✅ | `TestAuditLogApiKey` |
| 5 | API key avec filtre `actor_user_id` externe → résultat vide | ✅ | `test_audit_log_api_key_filters_forced` |
| 6 | Filtres `since`, `until`, `action`, `wallet_id` fonctionnent | ✅ | `TestAuditLogFilters` |
| 7 | Pagination cursor cohérente | ✅ | `TestAuditLogPagination` |
| 8 | `/actions` retourne la liste exhaustive | ⚠️ | Endpoint implémenté mais retourne les actions **distinctes en DB** (via `SELECT DISTINCT`) — pas la liste statique exhaustive de la spec. Au démarrage d'une instance fraîche, la liste serait vide. |
| 9 | Job purge supprime events > 90 jours | ✅ | `scripts/purge_audit_log.py` + tests |
| 10 | Job purge `--dry-run` ne supprime pas | ✅ | |
| 11 | Job purge tolère 10k+ events sans timeout | ✅ | Batch de 10k avec boucle |

---

## LOT 11 — UI Web

### Critères de succès

| # | Critère | Statut | Note |
|---|---------|--------|------|
| 1 | User se connecte via Keycloak | ✅ | OIDC configuré via `oidc-client-ts`, pages Login/OAuthCallback |
| 2 | First login : génère RSA, Argon2id, 24 mots, bootstrap | ✅ | `FirstLoginPage.tsx`, crypto modules présents |
| 3 | Se déconnecter et reconnecter avec passphrase | ✅ | `UnlockPage.tsx` |
| 4 | Recovery phrase fonctionne | ✅ | `RecoveryPhraseDisplay.tsx`, flux dans FirstLoginPage |
| 5 | Création wallet + secret + lecture round-trip | ✅ | Pages `WalletNewPage`, `SecretNewPage`, `SecretDetailPage` |
| 6 | Partage avec autre user fonctionne | ✅ | `GrantsPage.tsx` |
| 7 | Second user lit secrets partagés | ✅ | Couvert par le flux GrantsPage + SecretDetailPage |
| 8 | Création API key affiche token one-shot | ❌ | Page absente (`/wallets/:id/api-keys` non routé) |
| 9 | API key créée fonctionne avec SDK/CLI | ❌ | Bloqué par absence UI API keys |
| 10 | Export wallet → JSON sans valeurs | ❌ | Page absente |
| 11 | Import JSON → nouveau wallet en placeholders | ❌ | Page absente |
| 12 | Audit log filtrable | ✅ | `AuditLogPage.tsx` avec filtres et pagination |
| 13 | Change passphrase fonctionne | ✅ | `AccountPage.tsx` |
| 14 | Inactivity timeout déclenche lock | ✅ | `InactivityGuard.tsx`, 15 min |
| 15 | TypeScript strict, pas de `any` | ✅ | `tsconfig.json` strict |
| 16 | Mantine v7, design cohérent | ✅ | |
| 17 | Tests Playwright scénarios principaux passent | ❌ | Aucun test Playwright dans `frontend/src/tests/` (tests Vitest unitaires présents, mais pas E2E Playwright) |

### Écarts silencieux
- `WalletDetailPage` n'a qu'un seul onglet "secrets" — les onglets `grants`, `api-keys`, `settings`, `audit`, `export` prévus par la spec sont absents.
- Route `/wallets/:walletId/audit` (audit filtré par wallet) absente.

---

## Liste d'actions prioritaires

### P0 — Bloqueurs (le projet ne délivre pas sa promesse sans ces éléments)

| ID | Fichier(s) concerné(s) | Ce qui manque |
|----|----------------------|---------------|
| P0-01 | `backend/migrations/001_schema_initial.sql` | Tables manquantes : `user_external_identities`, `identity_anomaly_events`, `reverify_tokens`, `server_session_epoch`. Colonnes manquantes sur `users` : `quarantine_until`, `quarantine_reason`, `force_reverify_next_login`, `disabled_at`, `disabled_reason`. |
| P0-02 | `backend/app/api/v1/auth.py` | Endpoints entiers absents : `POST /me/reverify/challenge`, `POST /me/reverify`, `POST /me/quarantine/exit`, `GET/POST/DELETE/PUT /me/identities/*`, `GET /me/anomalies`, `POST /me/anomalies/{id}/acknowledge`. |
| P0-03 | `backend/app/core/security.py` (ou service dédié) | Détection d'anomalies au login absente (`detect_login_anomalies`), application quarantaine absente (`apply_quarantine_if_needed`), middleware `require_reverify` absent. |
| P0-04 | `frontend/src/App.tsx` + pages à créer | Routes et pages `/wallets/:id/api-keys`, `/wallets/:id/export`, `/wallets/import` entièrement absentes. Onglets manquants dans `WalletDetailPage.tsx`. |

### P1 — Should-have (lacunes significatives visibles en flux utilisateur)

| ID | Fichier(s) concerné(s) | Ce qui manque |
|----|----------------------|---------------|
| P1-01 | `backend/app/api/v1/config_public.py` | Champs `quarantine_inactivity_days` et `quarantine_duration_days` absents de la réponse (référencés dans la spec LOT_00 critère 3). |
| P1-02 | `backend/app/core/config.py` | Méthodes `Settings.get_sensitive_fields()` et `Settings.get_non_sensitive_fields()` absentes. Champs `is_secret` non taggés sur les Pydantic Fields. Ces méthodes servent à détecter programmatiquement les variables sensibles. |
| P1-03 | `backend/app/api/v1/audit_log.py` + `backend/app/db/repositories/audit_log.py` | `GET /v1/audit-log/actions` retourne les actions **présentes en DB** plutôt que la liste statique exhaustive de la spec. Sur instance fraîche, la liste est vide — l'UI ne peut pas peupler ses dropdowns. Corriger avec une constante statique. |
| P1-04 | `sdk-python/tests/e2e/` | Aucun test E2E SDK. L'`__init__.py` est vide. Les tests E2E (avec serveur de test) sont absents : `test_e2e_get_secret`, `test_e2e_populate_random`, `test_e2e_populate_all`, etc. |
| P1-05 | `frontend/src/pages/WalletDetailPage.tsx` | Onglets `grants`, `api-keys`, `settings`, `audit`, `export` manquants. La page n'expose qu'un seul onglet "secrets". |
| P1-06 | `cli-bash/tests/` | `smoke.sh` présent mais aucun test structuré. Les tests CLI bash de la spec (7 tests) ne sont pas écrits. |

### P2 — Nice-to-have (polish, accessibilité, cas limites)

| ID | Fichier(s) concerné(s) | Ce qui manque |
|----|----------------------|---------------|
| P2-01 | `backend/app/core/config.py` | Champ `age_public_key` absent de la config (prévu pour les backups lot 12). Non bloquant pour le MVP. |
| P2-02 | `frontend/src/` | Tests Playwright E2E absents (15 scénarios prévus dans la spec LOT_11). Les tests Vitest unitaires sont présents et couvrent le crypto. |
| P2-03 | `frontend/src/App.tsx` | Route `/wallets/:walletId/audit` (audit filtré par wallet) absente. |
| P2-04 | `sdk-python/harpocrate/cli.py` | Commande `info` aliasée vers `whoami` dans le CLI bash — `info` doit afficher les infos du wallet courant (nom, description, tags) via `GET /wallets/{wallet_id}`, distinctement de `whoami`. |
| P2-05 | `backend/app/api/v1/auth.py` | `PUT /me/passphrase` et `PUT /me/recovery` n'exigent pas de `X-Reverify-Token` (la table `reverify_tokens` est absente). À corriger quand P0-01/P0-02/P0-03 sont livrés. |

---

## Synthèse par LOT

| LOT | Titre | Backend | Frontend | Tests | Global |
|-----|-------|---------|----------|-------|--------|
| 00 | Fondations | ⚠️ | N/A | ✅ | ⚠️ |
| 01 | Schéma DB | ❌ | N/A | ⚠️ | ❌ |
| 02 | Auth + gouvernance | ❌ | N/A | ⚠️ | ❌ |
| 03 | Wallets CRUD | ✅ | ✅ | ✅ | ✅ |
| 04 | Grants | ✅ | ✅ | ✅ | ✅ |
| 05 | Secrets CRUD | ✅ | ✅ | ✅ | ✅ |
| 06 | Placeholders + générateurs | ✅ | ✅ | ✅ | ✅ |
| 07 | Export/Import | ✅ | ❌ | ✅ | ⚠️ |
| 08 | API keys | ✅ | ❌ | ✅ | ⚠️ |
| 09 | SDK Python + CLI Bash | ✅ | N/A | ⚠️ | ⚠️ |
| 10 | Audit log API | ✅ | ✅ | ✅ | ⚠️ |
| 11 | UI Web | N/A | ⚠️ | ⚠️ | ⚠️ |
